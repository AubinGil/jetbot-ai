# Eye gaze direction message
# Position can be: center, n, ne, e, se, s, sw, w, nw
# Or use custom x, y values (-1.0 to 1.0)

string position  # Named position (takes precedence if not empty)
float32 x        # Custom X position (-1.0 left to 1.0 right)
float32 y        # Custom Y position (-1.0 up to 1.0 down)
